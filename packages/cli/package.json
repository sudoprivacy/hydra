{
  "name": "@hydra/cli",
  "version": "0.3.2026071601",
  "description": "Hydra CLI — the `hydra` command (depends on @hydra/core)",
  "license": "MIT",
  "private": true,
  "main": "./out/cli/index.js",
  "bin": {
    "hydra": "./out/cli/index.js"
  },
  "scripts": {
    "compile": "tsc -b tsconfig.json"
  },
  "dependencies": {
    "@hydra/core": "*",
    "commander": "^14.0.3"
  }
}
